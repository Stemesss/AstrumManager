---
name: Project state
description: Текущий статус проекта, версия, этап — обновляется после каждого задания
---

# Текущее состояние проекта

## Статус

```
STATUS:   STABLE
VERSION:  1.2.8
DATE:     2026-07-02
PLATFORM: Replit (новый аккаунт, перенос 2026-07-02)
HEAD:     см. GIT SYNC ниже — обновляется после push
GIT SYNC: см. вывод `git ls-remote origin main` после push.sh
```

## Воркфлоу

| Процесс | Статус | Порт |
|---------|--------|------|
| Telegram Bot (Python / aiogram 3) | RUNNING | 6000 |
| API Server (Node.js / Express) | RUNNING | 8080 |
| Component Preview Server (Vite) | RUNNING | 8081 |

## Последнее задание

v1.2.8 — UI polish (без новой функциональности и архитектурных изменений):
- Приветственный экран `/start` для новых пользователей: интро-сообщение об AstrumManager
  перед мастером регистрации (только для действительно новых пользователей, `is_new=True`).
- Полное удаление легаси-титулов "Рекрут"/"Воин": единая схема ✪ Лидер / ✦ Дитя клана /
  ✧ Старейшина / ◇ Участник везде (build_full_nick, help-тексты, карточки участников).
- Редизайн карточки участника (главное меню + админ-раздел, общая `_card_text`):
  имя, роль (символ), игровой ник, Telegram, ID, дней в клане, статус.
- Кнопка «🔄 Полная синхронизация» в меню «Участники» — переиспользует существующую
  логику `/synctitles` (вынесена в `run_sync_titles()`), без новой бизнес-логики.
- Кнопка «🗑 Удалить участника» добавлена в карточку участника (админ-раздел).

## База данных

- Файл: `data/astrum.db`
- Движок: SQLite, WAL-режим ✅
- Таблицы: `users` (14), `audit_log` (26+), `news` (0), `forum_topics` (9),
  `publication_attachments` (0), `complaints` (0)

## Переменные окружения

- `TELEGRAM_BOT_TOKEN`  — ✅ установлен (Replit Secrets)
- `BOT_OWNER_ID`        — ✅ установлен (shared env var: 8490615925)
- `GITHUB_TOKEN`        — ✅ используется push.sh (найден автоматически)
- `AstrumManagerMain`   — ✅ установлен (fallback для push.sh)
- `WEBHOOK_SECRET`      — генерируется автоматически (не в Secrets — норма)
