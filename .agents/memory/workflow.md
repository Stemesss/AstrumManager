---
name: Workflow
description: Рабочий процесс разработки и текущий этап проекта
---

# Рабочий процесс проекта AstrumManager

## Текущий этап

```
ЭТАП:    Все плановые этапы завершены
СТАТУС:  STABLE
ВЕРСИЯ:  1.2.2
```

## Этапы проекта

```
[✅] Этап 1 — Critical   Базовая инфраструктура и запуск бота
[✅] Этап 2 — High       Пользователи, роли, ники
[✅] Этап 3 — Medium     Контент, аудит, администрирование
[✅] Этап 4 — Low        Статистика, полировка, стабилизация
[✅] Миграция            Перенос на новую среду Replit (2026-07-02)
```

## Рабочий процесс (SOP)

### Начало задания
1. Прочитать `AGENT_START.md`
2. Прочитать файлы памяти согласно порядку из `AGENT_START.md`
3. Выполнить обязательную проверку состояния (см. ниже)
4. Сообщить пользователю текущий статус
5. Ждать задания

### ОБЯЗАТЕЛЬНАЯ ПРОВЕРКА ПЕРЕД ЛЮБЫМ ИЗМЕНЕНИЕМ

До начала работы выполнить и сообщить:
```
1. git rev-parse --short HEAD          — текущий HEAD
2. git ls-remote origin main           — состояние origin/main
3. Совпадение HEAD и origin/main       — ДА / НЕТ
4. Версия проекта из project_state.md
5. git status                          — состояние рабочего дерева
6. Статус workflow: Telegram Bot / API Server (RUNNING / FAILED)
```
До окончания проверки — ничего не изменять.

### Выполнение задания
1. Проанализировать задание
2. Уточнить неясности (если есть)
3. Разбить задание на подэтапы (для планирования) и сгруппировать их в
   логические блоки: 2–3 связанных подэтапа или не более 6 изменённых файлов
4. Выполнять подэтапы внутри блока последовательно, без промежуточного
   push/checkpoint — только после завершения блока: полный цикл сохранения
5. Только после успешного push переходить к следующему блоку
6. Вывести итоговый отчёт в стандартном формате после финального блока

### УПРАВЛЕНИЕ КРЕДИТАМИ
Если кредиты могут закончиться в середине задачи:
- Не начинать следующий подэтап/блок
- Завершить текущий подэтап
- Выполнить полный цикл сохранения (даже если логический блок не закрыт)
- Дождаться Replit Checkpoint
- Выполнить push
- Отправить отчёт

Если Replit Checkpoint отсутствует:
- Не выполнять новые изменения
- Сообщить: «Ожидаю автоматический Replit Checkpoint для завершения цикла сохранения.»

### ОБЯЗАТЕЛЬНЫЙ ЦИКЛ СОХРАНЕНИЯ (после каждого логического блока)

Логический блок = 2–3 связанных подэтапа ИЛИ не более 6 изменённых файлов.
Внутри блока подэтапы выполняются без промежуточного push/checkpoint.

```
1. Обновить память проекта (project_state.md, change_log.md, etc.)
2. py_compile для всех изменённых .py файлов
3. Smoke-test: проверить логи webhook — HTTP 200, нет критических ошибок
4. Проверить все workflow (Telegram Bot / API Server — статус RUNNING)
5. Убедиться в создании Replit Checkpoint (платформа создаёт автоматически)
6. bash scripts/push.sh — push в origin/main
7. git ls-remote origin main — убедиться: origin/main == локальный HEAD
8. Только после п.7 — переходить к следующему блоку или считать задание завершённым
9. Только после п.7 — отправлять итоговый отчёт
```

**Если push НЕ выполнен** — запрещено писать «СТАТУС: выполнено».

### РЕГЛАМЕНТ ВЗАИМОДЕЙСТВИЯ С ПОЛЬЗОВАТЕЛЕМ

1. Обязательный план (название / цель / файлы / результат / риски) перед задачей,
   которая затрагивает более 4 файлов, имеет несколько логических частей или
   требует архитектурных изменений. После плана — стоп, ждать подтверждения.
2. Ошибка/регрессия обнаружена — запрещено продолжать новый функционал.
   Сначала: причина → исправление → проверка.
3. Несколько вариантов реализации — предложить пользователю, не выбирать самостоятельно.
4. Предлагать только функциональное развитие AstrumManager. Deploy/Git/ENV/Infrastructure
   — только по явному запросу.
5. Перед новой функцией — проверить существующий функционал. Расширять, не дублировать.
6. Все ответы — обычный текст, один блок text. Карточки/Canvas/Plan/таблицы — запрещены.
7. После каждого этапа — полный отчёт в установленном формате, все разделы обязательны.
8. «СТАТУС: выполнено» — только после синхронизации origin/main с локальным HEAD.

### После каждого задания обязательно обновить
- `project_state.md` — статус, последнее задание
- `change_log.md` — запись об изменении
- `audit.md` — если найден/исправлен дефект
- `workflow.md` — если изменился этап
- `PROJECT_HISTORY.md` — если завершён крупный этап
- `replit.md` — если изменилась версия

## Воркфлоу Replit

| Имя | Команда | Порт | Статус |
|-----|---------|------|--------|
| Telegram Bot | `python3 main.py` | 6000 | RUNNING |
| API Server | `pnpm --filter @workspace/api-server run dev` | 8080 | RUNNING |
| Component Preview Server | `pnpm --filter @workspace/mockup-sandbox run dev` | 8081 | RUNNING |

## Git-процесс

- git commit/add заблокированы в main agent — коммит = Replit Checkpoint (автоматически).
- Push выполняется через `bash scripts/push.sh` (поддерживает GITHUB_TOKEN и AstrumManagerMain).
- ВАЖНО: в main agent заблокированы `git add`, `git commit`, `git reset`.
  Push возможен только без force-флагов.
  (см. `.agents/memory/git-ops.md`)
